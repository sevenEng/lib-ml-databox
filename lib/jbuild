(jbuild_version 1)

(library
    ((name lib_databox)
     (public_name lib-databox)
     (preprocess (pps (ppx_bitstring)))
     (libraries (lwt-zmq bitstring hex uri base64 bos cohttp-lwt-unix ezjsonm))))
